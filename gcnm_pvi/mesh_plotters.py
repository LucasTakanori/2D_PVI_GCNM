# mesh_plotters.py  (STUB)
# Minimal stand-in so pvi_forward.py imports outside the main PVI repo.


class MeshPlotter2D:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "Stub: use the real mesh_plotters from the PVI repo for plotting."
        )
